%
% tikztemplate.tex -- template for standalon tikz images
%
% (c) 2021 Prof Dr Andreas Müller, Hochschule Rapperswil
%
\documentclass[tikz]{standalone}
\usepackage{amsmath}
\usepackage{times}
\usepackage{txfonts}
\usepackage{pgfplots}
\usepackage{csvsimple}
\usetikzlibrary{arrows,intersections,math}
\begin{document}
\def\skala{1}
\begin{tikzpicture}[>=latex,thick,scale=\skala]

\coordinate (q0) at (0,2);
\coordinate (p) at (-2.5,-2);
\coordinate (m) at (2.5,-2);
\coordinate (e) at (0,-1.5);
\coordinate (z) at (0,0);

\draw[->, shorten >= 0.3cm] (-1.5,2) -- (q0);
\draw (q0) circle[radius=0.3];
\node at (q0) {$q_0$};

\draw (p) circle[radius=0.3];
\draw (p) circle[radius=0.25];
\node at (p) {$p$};

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (q0) -- (p);
\node at (-1.4,0.2) [rotate={atan(4/2.5)}]
	{$\texttt{1},\dots,\texttt{9}$};

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm]
	(p) to[out=-90,in=-170,distance=1cm] (p);
\node at (-2.7,-2.5) [below] {$\texttt{0},\dots,\texttt{9}$};

\draw (m) circle[radius=0.3];
\node at (m) {$m$};

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (q0) -- (m);
\node at (1.2,0) [above right] {$\texttt{-}$};

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm]
	(m) to[out=-135,in=-45] (p);
\node at (0,-3) [below] {$\texttt{1},\dots,\texttt{9}$};

\node at (z) {$z$};
\draw (z) circle[radius=0.3];
\draw (z) circle[radius=0.25];

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (q0) -- (z);
\node at (0,1) [left] {$\texttt{0}$};

\node at (e) {$e$};
\draw (e) circle[radius=0.3];

\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (m) -- (e);
\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (p) -- (e);
\draw[->,shorten >= 0.3cm, shorten <= 0.3cm] (z) -- (e);
\draw[->,shorten >= 0.3cm, shorten <= 0.3cm]
	(e) to[out=-60,in=-120,distance=1cm] (e);

\node at (1.25,-1.8) [above] {$\texttt{0},\texttt{-}$};
\node at (-1.25,-1.8) [above] {$\texttt{-}$};
\node at (0,-2.2) [below] {$\Sigma$};
\node at (0,-0.75) [right] {$\Sigma$};

\end{tikzpicture}
\end{document}

